{% extends "base.html.twig" %}

{% block body %}
    <div class="users__title">
        Users List
    </div>
    <div class="users">
        <table class="users__table">
            <tr>
                <th class="users__table-head p">Id</th>
                <th class="users__table-head p">Email</th>
                <th class="users__table-head p">Roles</th>
                <th class="users__table-head p">Edit</th>
            </tr>
            {% if (users) %}
                {% for user in users %}
                    <tr>
                        <td class="users__table-item p">{{ user.id }}</td>
                        <td class="users__table-item p">{{ user.email }}</td>
                        <td class="users__table-item p">
                            {% for role in user.getRoles %}
                                {{ role }}<br>
                            {% endfor %}
                        </td>
                        <td class="users__table-item p">
                            <a href="{{ path('app_admin_user', {id: user.id }) }}">
                                <button class="button">Edit</button>
                            </a>
                        </td>
                    </tr>
                {% endfor %}
            {% endif %}
        </table>
    </div>
{% endblock %}